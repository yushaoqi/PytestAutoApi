# 公共参数
case_common:
  allureEpic: CSDN平台端
  allureFeature: 博客中心
  allureStory: 内容管理

csdn_api_01:
  host: ${{host}}
  url: /recommend/get_head_word
  method: POST
  detail: csdn的接口（没有开源接口，直接拿csdn的接口做演示）
  headers:
    Content-Type: application/json;charset=UTF-8
  # 请求的数据，是 params 还是 json、或者file
  requestType: json
  # 是否执行，空或者 true 都会执行
  is_run:
  data:
    applyId:
    reason: 审核时间${{get_time}}
    # 是否有依赖业务，为空或者false则表示没有
  dependence_case:
  # 依赖的数据
  dependence_case_data:
  assert:
    code:
      jsonpath: $.status
      type: ==
      value: 200
      AssertType:
  sql:
